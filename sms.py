# sms.py

def send_sms(phone: str, message: str):
    """
    فعلاً شبیه‌سازی ارسال پیامک
    بعداً به Kavenegar یا ملی‌پیامک وصل می‌شود
    """

    print(f"[SMS to {phone}]: {message}")

    return True